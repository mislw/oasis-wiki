interface:
  display_name: "Oasis Wiki"
  short_description: "Use Oasis UGC docs and safe MCP workflows."
  default_prompt: "Use $oasis-wiki for Oasis UGC Lua and UGCAskQ MCP work. Route config-driven UI through the DataTable -> Lua/RPC -> Widget refresh workflow, verify binary writes by readback and runtime logs, and use teaching mode only when explicitly requested."

policy:
  allow_implicit_invocation: true
